image: node:8.12.0

pages:
  cache:
    paths:
    - node_modules/

  stage: deploy
  script:
  - npm install -g @angular/cli@7.1.0
  - npm install
  - ng build --prod --output-path public --base-href "/soup-restaurant/"
  artifacts:
    paths:
    - public
  only:
  - master
  - modularize
